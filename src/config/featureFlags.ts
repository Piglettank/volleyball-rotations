/**
 * Toggle features here — set to `false` to hide UI without removing code.
 */
export const featureFlags = {
  /** Save / export / import layout buttons in the controls panel. */
  layoutPersistence: true,
} as const
